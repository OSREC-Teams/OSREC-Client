import styled from 'styled-components';

const ListElem = styled.tr`
  text-align: center;
  padding: 20px;
  width: 80%;

  &:hover {
    background-color: #7c7c7c;
    cursor: pointer;
  }
`;

export default ListElem;
